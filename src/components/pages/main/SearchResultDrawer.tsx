import React from 'react';
import { SwipeableEdgeDrawer } from '@/layouts';
const SearchResultDrawer = () => {
  return <SwipeableEdgeDrawer>SearchResultDrawer</SwipeableEdgeDrawer>;
};

export default SearchResultDrawer;
